# KAIRA Desktop Control Agent — Python dependencies
# Install with the Python 3.11 interpreter, e.g.:
#   "C:\Users\MSI\AppData\Local\Programs\Python\Python311\python.exe" -m pip install -r requirements.txt
#
# Trimmed 2026-08-19: dropped playwright (desktop browser automation),
# psutil + nvidia-ml-py3 (system/GPU info), pyperclip (clipboard), and
# pytesseract (screenshot OCR) — their tools were removed as unused bulk.
# This also means no more Chromium download/install step.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pyautogui==0.9.54
pywin32==308
Pillow==11.0.0
pycaw==20230407
pygetwindow==0.0.9
Send2Trash==1.8.3
python-multipart==0.0.20
